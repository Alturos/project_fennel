You will need to install Node.js and the following node modules in order to run this game:
Connect
Socket.io

Run the game by running the file "server/startup.js" in node.
For convenience, an http server is included at "server/test_server.js" and can also be run in node.